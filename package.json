{
  "name": "birkat-avraham",
  "private": true,
  "version": "1.0.0",
  "description": "אתר מוסדות \"ברכת אברהם\" — תורה, תפילה וחסד",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:staging": "vite build --mode staging",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "login": "npx firebase-tools login",
    "deploy": "npm run build && npx firebase-tools deploy --only hosting --project production",
    "deploy:all": "npm run build && npx firebase-tools deploy --project production",
    "deploy:rules": "npx firebase-tools deploy --only firestore:rules,storage --project production",
    "deploy:preview": "npm run build && npx firebase-tools hosting:channel:deploy preview --project production --expires 7d",
    "deploy:staging": "npm run build:staging && npx firebase-tools deploy --project staging"
  },
  "homepage": "https://birkat-avraham.com",
  "dependencies": {
    "firebase": "^12.17.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.4"
  },
  "devDependencies": {
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "gh-pages": "^6.3.0",
    "postcss": "^8.4.49",
    "prettier": "^3.9.6",
    "tailwindcss": "^3.4.17",
    "typescript": "^7.0.2",
    "vite": "^6.0.7"
  }
}
